# apif-assert

Assertion engine for .apif — AssertionEngine, PluginRegistry, JsonComparator

## Usage

```toml
[dependencies]
apif-assert = "1.7.0"
```

Part of the [grpctestify](https://github.com/gripmock/grpctestify-rust) workspace.
