# apif-plugins

Assertion plugins for .apif — PluginManager, uuid, email, regex, len, ip, url, timestamp

## Usage

```toml
[dependencies]
apif-plugins = "1.7.0"
```

Part of the [grpctestify](https://github.com/gripmock/grpctestify-rust) workspace.
